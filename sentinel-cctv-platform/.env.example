# GujRaksha (ગુજ રક્ષા) — Production PostgreSQL & Platform Configuration
PORT=3000
NODE_ENV=development

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sentinel_cctv_db
DB_USER=postgres
DB_PASSWORD=postgres

# Alternative: Direct PostgreSQL Connection URL (e.g., Supabase / Neon / AWS RDS / Local)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sentinel_cctv_db

# Security & Tokens
JWT_SECRET=sentinel-gujarat-police-secret-key-2026
ENCRYPTION_KEY=sentinel-aes256-secret-encryption-key-32b
